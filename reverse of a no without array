#include <stdio.h>

int
main ()
{
  int n, i, temp, rev = 0;
  scanf ("%d", &n);
  while (n != 0)
    {
      temp = n % 10;
      rev=rev*10;
      rev = rev + temp;
      n = n / 10;
    }

  printf ("%d", rev);

  return 0;
}
